# ============================================================
# 君斗りんくのすれ違ひ通信 環境変数サンプル
# このファイルをコピーして .env.local を作成してください。
# .env.local は .gitignore に含まれているため、Git には含まれません。
# ============================================================

# ------------------------------------------------------------
# Supabase (Postgres) — 接続文字列
# 取得手順:
#   1. https://supabase.com でプロジェクトを作成
#   2. プロジェクト設定 > Database > Connection string > URI を選択
#   3. "Transaction pooler" (ポート 6543) を使用（Vercel Functions 向け）
#      例: postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres
#   4. 以下の DATABASE_URL に貼り付ける
# ------------------------------------------------------------
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ------------------------------------------------------------
# Clerk (認証)
# https://dashboard.clerk.com > プロジェクト > API Keys
# ------------------------------------------------------------
# ★重要（2026-08 kimito.link Clerk 統合）:
#   ここに入れる鍵は **kimito.link アプリ（primary）のもの**。
#   本番は pk_live_ / sk_live_（surechigai 独自アプリ surechigai-nico.link の鍵ではない）。
#   これにより surechigai が kimito.link と同じ Clerk アカウント基盤に入る（1ログインで全サービス）。
#   鍵の取得と Satellite ドメイン登録は Clerk Dashboard で人間が実施:
#   → リポ直上 KIMITO-CLERK-UNIFICATION-PLAN.md 参照。
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...   # kimito.link アプリのもの
CLERK_SECRET_KEY=sk_live_...                     # kimito.link アプリのもの

# --- Clerk Satellite: kimito.link を primary IdP として共有する ---
#  (本命) *.kimito.link で配信(例: surechigai.kimito.link):
#      同一親ドメインなので Cookie が .kimito.link で共有され、シームレスに 1 ログインが成立。
#      Clerk Dashboard(kimito.link アプリ) の allowedRedirectOrigins に当該サブドメインを含めること。
#
#  (別ドメイン配信の保険) surechigai-romi.link 等の別ドメイン:
#      satellite props（isSatellite/domain）で primary に委譲。lib/clerk-provider-props.ts が
#      下記 env を読んで自動で satellite モードにする（env が無ければ従来の単独動作＝安全側）。
#
# 本番で satellite を有効化する env（Vercel に設定）:
EXPO_PUBLIC_CLERK_IS_SATELLITE=true
EXPO_PUBLIC_CLERK_DOMAIN=clerk.kimito.link
EXPO_PUBLIC_CLERK_SIGN_IN_URL=https://kimito.link/sign-in/

# ------------------------------------------------------------
# Twitter / X OAuth 2.0
# https://developer.twitter.com/en/portal/dashboard
# Callback URL: https://clerk.kimito.link/v1/oauth_callback
# (開発時: http://localhost:8081/oauth/twitter-callback)
# ------------------------------------------------------------
TWITTER_CLIENT_ID=...
TWITTER_CLIENT_SECRET=...

# ------------------------------------------------------------
# セキュリティ
# openssl rand -hex 32 で生成
# ------------------------------------------------------------
ENCRYPTION_KEY=your-64-char-hex-key-here
SESSION_SECRET=your-random-session-secret

# ------------------------------------------------------------
# サーバー設定
# ------------------------------------------------------------
NODE_ENV=development
PORT=3000

# ------------------------------------------------------------
# スイープ Webhook セキュリティトークン
# GitHub Actions から /api/sweep を叩くときの認証トークン
# openssl rand -hex 32 で生成
# GitHub Actions secrets の SWEEP_SECRET と同じ値を設定する
# ------------------------------------------------------------
SWEEP_SECRET=your-sweep-secret-here

# ------------------------------------------------------------
# API ベース URL (GitHub Actions スイープ用)
# 本番: https://surechigai.kimito.link
# 開発: http://localhost:3000
# ------------------------------------------------------------
API_BASE_URL=http://localhost:3000

# ------------------------------------------------------------
# Sentry (エラー監視) — オプション
# ------------------------------------------------------------
# SENTRY_DSN=https://...

# ------------------------------------------------------------
# Groq API (ひとことモデレーション)
# https://console.groq.com
# ------------------------------------------------------------
# GROQ_API_KEY=gsk_...

# ------------------------------------------------------------
# Gemini API (演出文・モデレーション控え)
# https://aistudio.google.com
# ------------------------------------------------------------
# GEMINI_API_KEY=AIza...

# ------------------------------------------------------------
# OGP 地図サムネ (/api/og) のタイル提供元 — オプション
# 未設定なら OSM 標準タイル（キー不要）を使用。
# MapTiler のキーを設定すると実写寄りの高品質タイルに自動で切り替わる。
# https://cloud.maptiler.com/account/keys/ （無料枠あり）
# MAPTILER_STYLE は任意（既定 streets-v2。例: hybrid で航空写真寄り）
# ------------------------------------------------------------
# MAPTILER_KEY=your-maptiler-key
# MAPTILER_STYLE=streets-v2
