# v6.51 本番確認結果

## 1. /api/health?schema=true

レスポンス:
```json
{"ok":true,"timestamp":1769023795607,"version":"unknown","gitSha":"unknown","builtAt":"unknown","nodeEnv":"production"}
```

**問題点**: `schema` フィールドが返されていない
→ v6.51のコードがまだ本番にデプロイされていない

本番はまだv6.43のコードで動作している（画面に「v6.43」と表示）。
v6.51（スキーマチェック機能追加）はGitHubにプッシュされていないため、Railwayにデプロイされていない。

## 2. /event/90001

イベント詳細ページは表示されず、初回訪問の「はじめまして！」画面が表示される。
→ イベントページが読み込まれる前にオンボーディングが表示されている。

**重要**: 本番はまだv6.43。deletedAt/deletedByカラムは既に適用済みだが、
コードがv6.43のため、ソフトデリート機能は動作しない。

## 結論

本番DBのマイグレーションは完了しているが、コードのデプロイが必要。
GitHubにプッシュしてRailwayの自動デプロイをトリガーする必要がある。
