# 引き継ぎ 2026-07-11 — Grok監査全対応+重大バグ2件修正完了後の続き

> 前セッション成果: Grok監査(P0/P1/P2)全クローズ・本番重大バグ2件を発見修正・LP増補「道連れと見本の巻」マージ済み。
> 本番 = main 46c9ac5a5(version.json一致確認済)。テスト316件全緑。ゲスト/認証の回帰なしを実測済み。

## 完了済み(触らない・再調査しない)

| 項目 | 状態 |
|---|---|
| P0-1 チェックイン偽成功 | 修正済(saved:true肯定形判定+サーバーthrow)。契約テストあり |
| P1-4 !db偽成功12箇所 | requireDb()規約で一掃 |
| P1-2 429素JSON | tRPCエンベロープ化(認証ユーザー単位キーは別セッションが追加) |
| P1-8 Sentry | EXPO_PUBLIC_SENTRY_DSN(client)+SENTRY_DSN(server)設定済み。配信バンドルにDSN焼込を実測確認。**数日後にbesttrust/javascript-reactのIssuesを覗く** |
| encounter.list 常時500 | **修正済**。生sqlテンプレートにDate直渡し→lt()演算子へ(b4c6d120f)。生sql+Dateは禁止 |
| **タブ直アクセスが/に落ちる** | **修正済**(c87916dec→46c9ac5a5)。認証プロバイダchunk解決時のラッパー差し替えでReactがナビ一式を再マウント→状態リセットが機序。RestoreDeepLinkAfterAuthBoot(app/_layout.tsx)がpathname変化検知で復元。**5タブ直アクセスOK・ゲスト回帰なしを本番実測済み** |
| TrpcReadyデフォルトtrue地雷 | プレースホルダ期間にTrpcReadyProvider(false)配布で修正 |
| LP道連れと見本の巻(P1〜P4b) | Codex実装をマージ・本番反映(05fa6dbd6)。判定全green |
| auth-state | Patchright**永続プロファイル**方式で再取得済(.auth/auth-state.json, 7/11)。X再ログインは今後profile再利用でほぼ不要 |

## 残タスク(優先順)

1. **checkin-tap E2Eの安定化**(tests/e2e/trail-auth.smoke.spec.ts)
   - 現状: 軌跡テストはpass。チェックインタップだけRNWボタンのactionabilityが不安定で赤
   - 既知の欠陥: 検証regex `/位置を取得中|記録中|取得中/` がサイドバー「1人が記録中」に**常時偽マッチ**する→ `位置を取得中` 等に絞る+`main`領域スコープ化
   - クリックは `page.mouse` 座標方式 or `dispatchEvent` の検討。成功すればidolfunchの実チェックイン1件が入り、skip中の削除/公開切替テストも解放される
2. **実データ「今日の灯」**(追体験会議の次候補): 認証不要の公開集計API(県別チェックイン数)をアプリ側に新設→LPに表示。捏造回避で集計値のみ。設計はメモリ[surechigai-lp-feature-appeal-spec]参照
3. P2低優先: P2-2(/event/[id]ナビ定義のみ)・P2-4(/u/*ログイン導線handoff欠落調査)・P2-6(legacy twitter-routes削除)
4. 実機確認(ユーザー): ログイン済みスマホで `surechigai.kimito.link/map` 直開き→軌跡に着地するか

## 地雷(必読・破ると事故)

- **ラッパー差し替え=子の再マウント**は構造的に不可避。app/_layout.tsxの認証ブート構造を触るならRestoreDeepLinkAfterAuthBootの機序コメントを先に読む
- drizzleで**生sqlテンプレートにDate/値を直渡ししない**(型マッピング迂回でpostgres.jsが死ぬ)。演算子(lt/gte等)経由必須
- vitestでreact-nativeをimportするコードは`vi.mock("react-native",...)`必須(Flow構文でrollupが死ぬ)
- クライアントバンドルを触ったらCDN_CACHE_EPOCH+1(theme/tokens/index.ts, 現在2)
- LP: overflowはclipのみ/画像・動画に?v=禁止(ファイル名版数)/mokuroku li=31不変/検証はsettleTo反復スクロール方式
- E2Eは`PLAYWRIGHT_BASE_URL=https://surechigai.kimito.link`で本番向け実行(ローカルExpoは起動120秒TO)
- auth-stateの保存先が親github/.authになる罠(.tmpスクリプトのROOT計算)→リポ.auth/へコピー

## 参照
- メモリ: surechigai-fake-success-purge(監査対応+バグ機序の全記録) / surechigai-lp-feature-appeal-spec / surechigai-patchright-x-oauth-auth
- 設計正本: docs/lp-michizure-mihon-DESIGN.md / docs/lp-feature-appeal-DESIGN.md
