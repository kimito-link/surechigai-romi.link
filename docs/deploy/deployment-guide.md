# デプロイガイド

**最終更新**: 2026-01-29

このドキュメントは、君斗りんくの動員ちゃれんじのデプロイプロセスを詳細に説明します。

---

## 🚨 重要な前提条件

### 1. GitHub認証が必須

**問題**: `webdev_save_checkpoint`はS3内部リポジトリには保存できるが、GitHubへの同期には**GitHub認証が必要**。

**解決策**: GitHub Personal Access Token (PAT)を使用してGitHubにpushする。

### 2. デプロイワークフローが必須

**問題**: `.github/workflows/deploy-vercel.yml`が存在しないと、GitHubへのpush後も自動デプロイが実行されない。

**解決策**: デプロイワークフローを作成し、GitHubにpushする。

---

## 📋 デプロイ手順（標準）

### Step 1: コードの変更とチェックポイント保存

```bash
# 1. コードを変更
# 2. webdev_save_checkpointを実行（Manus AI環境）
```

**結果**: S3内部リポジトリにコミットが保存される。

---

### Step 2: GitHubへのpush

**重要**: `webdev_save_checkpoint`だけではGitHubに反映されない。必ず以下を実行する。

```bash
cd /home/ubuntu/birthday-celebration

# GitHub認証の確認
gh auth status

# 認証が切れている場合、PATを使用
git remote set-url github https://kimito-link:<GITHUB_TOKEN>@github.com/kimito-link/doin-challenge.com.git

# GitHubにpush
git push github main

# 成功確認
echo "✅ pushed to GitHub" || (echo "❌ GitHub push failed" && exit 1)
```

**結果**: GitHubのmainブランチが更新される。

---

### Step 3: GitHub Actionsの自動実行

**前提条件**: `.github/workflows/deploy-vercel.yml`が存在すること。

```yaml
# .github/workflows/deploy-vercel.yml の内容
name: Deploy to Vercel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
      
      - name: Install dependencies
        run: pnpm install
      
      - name: Build
        run: pnpm build
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
```

**結果**: Vercelに自動デプロイされる。

---

### Step 4: デプロイ確認

```bash
# 本番環境のバージョン確認
curl -s https://doin-challenge.com/api/health | jq

# GitHubの最新コミットと一致しているか確認
cd /home/ubuntu/birthday-celebration
git log --oneline -n 1
```

**期待される結果**:

```json
{
  "version": "8f20d95...",
  "commitSha": "8f20d95..."
}
```

---

## 🔧 トラブルシューティング

### 問題1: GitHubにpushできない

**症状**: `fatal: Authentication failed`

**原因**: GitHub認証が切れている

**解決策**:

1. GitHub PATを作成（https://github.com/settings/tokens）
   - Scopes: `repo`, `workflow`
   - Expiration: 90 days以上

2. PATを使用してpush
   ```bash
   git remote set-url github https://kimito-link:<GITHUB_TOKEN>@github.com/kimito-link/doin-challenge.com.git
   git push github main
   ```

---

### 問題2: GitHub Actionsが実行されない

**症状**: GitHubにpushしたが、Actionsページに新しい実行が表示されない

**原因**: `.github/workflows/deploy-vercel.yml`が存在しない

**解決策**:

1. デプロイワークフローを作成（上記のYAMLを参照）
2. GitHubにpush
   ```bash
   git add .github/workflows/deploy-vercel.yml
   git commit -m "Add deploy workflow"
   git push github main
   ```

---

### 問題3: Vercelにデプロイされない

**症状**: GitHub Actionsは成功したが、本番環境が更新されない

**原因**: Vercelの環境変数が設定されていない

**解決策**:

1. GitHubリポジトリのSecretsを確認
   - `VERCEL_TOKEN`
   - `VERCEL_ORG_ID`
   - `VERCEL_PROJECT_ID`

2. Vercel CLIで直接デプロイ（緊急回避）
   ```bash
   pnpm dlx vercel --prod
   ```

---

## 📊 デプロイフロー図

```
┌─────────────────────────────────────────────────────────────┐
│ 1. コード変更 + webdev_save_checkpoint                       │
│    → S3内部リポジトリに保存                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. git push github main                                      │
│    → GitHubのmainブランチが更新                               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. GitHub Actions自動実行                                     │
│    → .github/workflows/deploy-vercel.ymlが実行される          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Vercel自動デプロイ                                         │
│    → https://doin-challenge.comが更新される                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. デプロイ確認                                               │
│    → curl https://doin-challenge.com/api/health | jq         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 チェックリスト

デプロイ前に以下を確認してください：

- [ ] `shared/version.ts`のバージョンが更新されている
- [ ] `webdev_save_checkpoint`が成功している
- [ ] `git push github main`が成功している
- [ ] GitHub Actionsが実行されている
- [ ] Vercelにデプロイされている
- [ ] `/api/health`のバージョンが最新コミットと一致している

---

## 📚 関連ドキュメント

- [README.md](../README.md) - プロジェクト概要とAIへの指示
- [chatlog-20260129.md](./chatlog-20260129.md) - デプロイ問題の詳細ログ
- [GitHub Actions](https://github.com/kimito-link/doin-challenge.com/actions) - ワークフロー実行履歴
- [Vercel Dashboard](https://vercel.com/kimito-link/doin-challenge-com) - デプロイ状況

---

## 🔐 セキュリティ注意事項

1. **GitHub PATは機密情報** - 絶対に公開しない
2. **Vercel Tokenも機密情報** - GitHub Secretsに保存する
3. **ログにトークンを出力しない** - デバッグ時は`***`でマスクする

---

## 📝 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2026-01-29 | v1.0 | 初版作成 - GitHub認証問題とデプロイワークフロー欠落の対応 |
