# PR-3 レビューチェックリスト

## Phase 2実装ガイド準拠チェック

### ✅ NG集チェック（触ってはいけないファイル）

- [x] `app/oauth/**` に触れていない
- [x] `server/twitter*` に触れていない
- [x] `hooks/use-auth.ts` に触れていない
- [x] `app/(tabs)/index.tsx` の既存ログインボタンに触れていない

### ✅ FSM状態遷移チェック

- [x] `waitingReturn` 状態の定義が正しい
  - `startedAt: number` フィールドあり
  - `timeoutMs: number` フィールドあり
- [x] `redirecting → waitingReturn` の遷移が実装されている
- [x] タイムアウト処理が実装されている（30秒）
- [x] タイムアウト後に `cancel` 状態に遷移する

### ✅ コンポーネントチェック

- [x] `WaitingReturnScreen` コンポーネントが作成されている
- [x] りんくの吹き出しが使われている
- [x] 「もう少しだよ！」メッセージが表示される
- [x] ローディングインジケーターが表示される
- [x] 残り時間のカウントダウンが表示される
- [x] 絵文字が使われていない

### ✅ 統合チェック

- [x] `GlobalMenu` に `WaitingReturnScreen` が統合されている
- [x] `auth-ux/index.ts` に export が追加されている
- [x] 状態に応じて表示/非表示が切り替わる

### ✅ テストチェック

- [x] ユニットテストが更新されている
- [x] `waitingReturn` 状態への遷移テストがある
- [x] タイムアウト処理のテストがある
- [x] 全テストがパスしている（9テスト）

### ✅ 文言チェック

- [x] 「認証」「OAuth」「コールバック」などの技術用語を使っていない
- [x] 「ログイン」「X画面」「戻ってくる」などのユーザーフレンドリーな文言を使っている
- [x] 絵文字を使っていない

## レビュー結果

**✅ 全チェック項目クリア**

PR-3の実装はPhase 2実装ガイドに準拠しており、問題ありません。

## 次のステップ

- [ ] レスポンシブチェック（375px/768px/1024px）
- [ ] 手動1分儀式実行
- [ ] PR-4（success状態）の実装
