# Play Console 手作業チェックリスト（すれ違ひ通信・2026-08-11）

内部テストへの配信は完了済み（`versionCode 588`）。**残るのは Play Console の UI 作業だけ**。

ここに書いた回答は、**このアプリのコードを実測して確定**させたもの。そのまま入力してよい。

- ダッシュボード: https://play.google.com/console/u/0/developers/6880871170619890401/app/4974108202924371469/app-dashboard
- パッケージ名: `com.kimito.link.surechigai`

> ⚠️ 各項目を保存すると「[公開の概要] に移動しますか?」と聞かれる。
> **全部終わるまで「後で行う」**を選ぶこと。
> 直リンクURLは Play Console の SPA がアプリ一覧に弾くので、必ずサイドバーから辿る。

---

## 自動で入っている項目（触らなくてよい）

CI と API で投入済み。UI で再入力しないこと。

| 項目 | 状態 |
|---|---|
| アプリ名・簡単な説明・詳しい説明（ja-JP） | ✅ 投入済み |
| アイコン512・フィーチャーグラフィック・スクショ5枚 | ✅ 投入済み |
| AAB（内部テスト） | ✅ `versionCode 588` |
| リリースノート | ✅ 手書きの初回リリース文 |
| **データセーフティ（約50問）** | ✅ **API で投入済み** |

---

## 手作業が要る項目

`ポリシーとプログラム → アプリのコンテンツ` から順に埋める。

### 1. プライバシーポリシー
```
https://surechigai.kimito.link/privacy
```

### 2. アプリのアクセス権（ログインの詳細）
**「一部の機能に制限あり」** を選ぶ。

このアプリは**チェックイン・図鑑・マイページがログイン必須**（X または Apple の OAuth）。
デモアカウントを登録するか、下の手順を「指示」欄に書く。

> ⚠️ 審査用のデモアカウントは Secrets に未登録（`IOS_REVIEW_DEMO_*` が無い）。
> X アカウントを1つ用意して渡すか、審査員に「Xアカウントでログインしてください」と案内する。
> トップページ・地図・イベント一覧は**ログイン不要で見える**ので、その旨も添えるとよい。

### 3. 広告
**「広告は含まれていません」**

根拠: 広告SDK は `package.json` に **0件**（AdMob 等なし）。
アプリ内に出る協賛カードは自社DBから配信する自前のもので、広告ネットワークではない。

### 4. コンテンツのレーティング（IARC アンケート）
- メールアドレス: 連絡先メール
- カテゴリ: **「その他すべてのアプリ」**（ソーシャル/コミュニケーション扱いでも可）
- 暴力・性的表現・冒とく的表現・薬物 … **すべて「いいえ」**
- **ユーザー同士が交流できる**: **はい**（すれ違い・スタンプ）
- **ユーザーの現在地を共有できる**: **はい** ← このアプリの中核なので正直に
- 個人情報の共有: **はい**（Xアカウント名が公開範囲に応じて見える）

結果は全年齢〜12+ 相当になる見込み（`app.config.json` は `TWELVE_PLUS`）。

### 5. ターゲットユーザーと年齢層
**13〜15 / 16〜17 / 18歳以上**（13歳以上の全層）にチェック。

根拠: ログインに X が必要で、X の利用規約が13歳以上。子供向けアプリにはしない。

### 6. 広告ID（AAID）
**「使用しない」**

> ⚠️ 項目3で「広告なし」と答えていても、**審査送信の直前に別ダイアログで聞かれる**。
> ここを埋めないと送信できない（キットの playbook §①.6 の実体験）。

### 7. 行政アプリ
**いいえ**

### 8. 金融取引機能
最下部の **「このアプリは金融取引機能を提供していません」** にチェック。

根拠: 課金SDK 0件・決済情報のカラムなし。

### 9. 健康
最下部の **「アプリに健康関連の機能はない」** にチェック。

### 10. ストアの設定（カテゴリと連絡先）
- カテゴリ: **ライフスタイル**（`app.config.json` の `primaryCategory: LIFESTYLE`）
- メール / ウェブサイト: 既存の値のまま

---

## 製品版として公開するとき

内部テストには無い罠がある（キット playbook §①.5 の実体験）。

### 配信国/地域の選択【必須】
`テストとリリース → 製品版 → 国/地域` タブ → 「国/地域を追加」→ **日本**にチェック → 保存。

> これを忘れると、レビュー画面で
> 🔴「このトラックの国または地域が選択されていません」
> が出て**保存ボタンがグレーのまま**になる。内部テストでは不要なので初見で必ず詰まる。

### 送信
全項目を埋めると「アプリのコンテンツ」の要注意が **0** になり、
`公開の概要` から審査に送信できるようになる。

---

## 送信後

配信開始が確認できたら、**私に伝えてください**。以下を実施します。

1. `app.config.json` の `stores.playAppId` に `4974108202924371469` を設定
2. これだけで `/u/{slug}` の共有ページに **Google Play ボタンが自動で出る**（コード変更不要）

> 公開前に入れると、押した人が「見つかりません」に着地するため、順序を守る。
