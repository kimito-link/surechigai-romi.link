# {プロジェクト名} NG集

**このファイルは、やってはいけないことをまとめたものです。**

PRレビュー時に必ずチェックしてください。

---

## 目次

1. [触ってはいけないファイル](#触ってはいけないファイル)
2. [使ってはいけないライブラリ/関数](#使ってはいけないライブラリ関数)
3. [禁止されている実装パターン](#禁止されている実装パターン)
4. [禁止ワード](#禁止ワード)
5. [過去に問題を起こした実装](#過去に問題を起こした実装)

---

## 触ってはいけないファイル

以下のファイルは、特別な理由がない限り変更してはいけません。

### 1. `{ファイル名またはディレクトリ}`

**理由**: {理由（例: 本番環境で動作している重要なファイル）}

**例外**: {例外がある場合（例: バグ修正の場合のみ可）}

**代替案**: {代替案（例: 新しいファイルを作成して機能を追加）}

### 2. `{ファイル名またはディレクトリ}`

**理由**: {理由}

**例外**: {例外がある場合}

**代替案**: {代替案}

### 3. `{ファイル名またはディレクトリ}`

**理由**: {理由}

**例外**: {例外がある場合}

**代替案**: {代替案}

---

## 使ってはいけないライブラリ/関数

### 1. `{ライブラリ名または関数名}`

**理由**: {理由（例: セキュリティリスクがある、パフォーマンスが悪い）}

**代替案**: {代替案（例: ○○ライブラリを使う）}

**NG例**:
```typescript
// NG: {理由}
import { badFunction } from 'bad-library';
badFunction();
```

**OK例**:
```typescript
// OK: {理由}
import { goodFunction } from 'good-library';
goodFunction();
```

### 2. `{ライブラリ名または関数名}`

**理由**: {理由}

**代替案**: {代替案}

**NG例**:
```typescript
// NG: {理由}
// 悪い実装
```

**OK例**:
```typescript
// OK: {理由}
// 良い実装
```

---

## 禁止されている実装パターン

### 1. {パターン名（例: グローバル状態への直接アクセス）}

**理由**: {理由（例: テストが困難になる、デグレードリスクが高い）}

**NG例**:
```typescript
// NG: {理由}
const globalState = window.globalState;
globalState.user = newUser;
```

**OK例**:
```typescript
// OK: {理由}
const { setUser } = useUserContext();
setUser(newUser);
```

### 2. {パターン名（例: any型の使用）}

**理由**: {理由（例: 型安全性が失われる）}

**NG例**:
```typescript
// NG: any型を使用
function processData(data: any) {
  return data.value;
}
```

**OK例**:
```typescript
// OK: 型定義を明確にする
interface Data {
  value: string;
}

function processData(data: Data) {
  return data.value;
}
```

### 3. {パターン名（例: 非同期処理のawait忘れ）}

**理由**: {理由（例: レースコンディションが発生する）}

**NG例**:
```typescript
// NG: awaitを忘れる
async function fetchData() {
  const data = fetchFromAPI(); // awaitがない
  console.log(data); // Promiseが表示される
}
```

**OK例**:
```typescript
// OK: awaitを使う
async function fetchData() {
  const data = await fetchFromAPI();
  console.log(data); // データが表示される
}
```

### 4. {パターン名（例: useEffectの依存配列忘れ）}

**理由**: {理由（例: 無限ループが発生する）}

**NG例**:
```typescript
// NG: 依存配列がない
useEffect(() => {
  fetchData();
}); // 依存配列がない → 無限ループ
```

**OK例**:
```typescript
// OK: 依存配列を指定
useEffect(() => {
  fetchData();
}, []); // 空配列 → 初回のみ実行
```

---

## 禁止ワード

以下の言葉は、ユーザー向けテキストで使ってはいけません。

| 禁止ワード | 理由 | 代替案 |
|-----------|------|--------|
| {ワード1（例: 絶対）} | {理由（例: 誇張表現で法務リスク）} | {代替案（例: 多くの場合）} |
| {ワード2（例: 完璧）} | {理由} | {代替案} |
| {ワード3（例: 保証）} | {理由} | {代替案} |

**NG例**:
```typescript
// NG: 禁止ワードを使用
<Text>このアプリを使えば絶対に成功します！</Text>
```

**OK例**:
```typescript
// OK: 代替案を使用
<Text>このアプリを使うと、成功の可能性が高まります。</Text>
```

---

## 過去に問題を起こした実装

### 1. {問題の説明（例: ログイン処理でトークンが保存されない）}

**発生日**: {日付（例: 2024-01-15）}

**原因**: {原因（例: AsyncStorageの非同期処理をawaitしていなかった）}

**対策**: {対策（例: AsyncStorageの全ての呼び出しにawaitを追加）}

**NG例**:
```typescript
// NG: awaitがない
AsyncStorage.setItem('token', token);
navigate('/home'); // トークンが保存される前に遷移
```

**OK例**:
```typescript
// OK: awaitを使う
await AsyncStorage.setItem('token', token);
navigate('/home'); // トークンが保存された後に遷移
```

### 2. {問題の説明}

**発生日**: {日付}

**原因**: {原因}

**対策**: {対策}

**NG例**:
```typescript
// NG: 悪い実装
```

**OK例**:
```typescript
// OK: 良い実装
```

### 3. {問題の説明}

**発生日**: {日付}

**原因**: {原因}

**対策**: {対策}

**NG例**:
```typescript
// NG: 悪い実装
```

**OK例**:
```typescript
// OK: 良い実装
```

---

## チェックリスト

PRレビュー時に、以下をチェックしてください。

- [ ] 触ってはいけないファイルに触れていない
- [ ] 使ってはいけないライブラリ/関数を使っていない
- [ ] 禁止されている実装パターンを使っていない
- [ ] 禁止ワードを使っていない
- [ ] 過去に問題を起こした実装を繰り返していない

---

## NG集の更新

新しいNG項目が見つかった場合、以下の手順で更新してください。

1. **問題を特定**: どのような問題が発生したか
2. **原因を分析**: なぜ問題が発生したか
3. **対策を決定**: どのように防ぐか
4. **NG集に追加**: このファイルに追記
5. **チームに共有**: Slackやメールで共有

---

## まとめ

このNG集を守ることで、以下のメリットがあります。

1. **デグレードを防ぐ**: 過去の失敗を繰り返さない
2. **開発速度向上**: レビューで指摘される回数が減る
3. **品質向上**: バグが減る
4. **安心感**: 「どこか直したら他が壊れる」問題が解消される

**実装前に必ずこのNG集を読んでください！**
